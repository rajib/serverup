$("#form_search_contacts").each(function(){
    this.reset();
});
$('#form_search_contacts').attr('action','/servers/'+<%= @server.id %>+'/contacts_servers/');
$("#head_javascript").html("<%= escape_javascript(render partial: 'servers/head_javascript_content', locals: {server: @server}) %>");
$("#already_added_contacts").html("<%= escape_javascript(render partial: 'servers/contacts', locals: {server: @server}) %>");
$('#table_already_added_contacts').dataTable({
         "sDom": "<'row'<'span6'f><'span6'>r>t<'row'<'span12'p>>",
         "sPaginationType": "bootstrap",
         "aaSorting": []
});