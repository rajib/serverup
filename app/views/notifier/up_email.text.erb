The Following Server is Up:-
=====================================================================
<%= @server.name %>(<%= @server.url %>) is UP again at <%= @server.updated_at.strftime("%d-%m-%y") %>  <%= @server.updated_at.strftime("%H:%M:%S%p") %>.

