<%= simple_form_for(@contact, remote:true) do |f| %>
  	<%= f.input :name %>
  	<%= f.input :email %>
        
  	<div class="form-actions">
      	<%= f.button :submit, class:'btn btn-primary' %>
      </div>
<% end %>